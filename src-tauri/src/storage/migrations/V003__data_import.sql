-- V003__data_import.sql
-- This migration is a placeholder. The actual data import is handled
-- in Rust code (see db.rs migration_from_files) because we need to
-- read JSON files and transform data programmatically.
-- This file ensures the migration version is tracked.

-- No SQL operations needed; data import is done in Rust.
SELECT 1;
